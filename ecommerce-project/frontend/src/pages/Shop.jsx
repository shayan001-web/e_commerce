import { useEffect, useState } from 'react'; import { useSearchParams } from 'react-router-dom'; import { SlidersHorizontal } from 'lucide-react'; import api from '../services/api'; import ProductCard from '../components/ProductCard';
export default function Shop() { const [params, setParams] = useSearchParams(); const [products, setProducts] = useState([]); const [categories, setCategories] = useState([]); const [loading, setLoading] = useState(true); const [total, setTotal] = useState(0); const search = params.get('search') || ''; const category = params.get('category') || ''; const sort = params.get('sort') || 'newest'; const update = (key, value) => { const next = new URLSearchParams(params); value ? next.set(key, value) : next.delete(key); setParams(next); }; useEffect(() => { setLoading(true); Promise.all([api.get(`/products?limit=12&search=${encodeURIComponent(search)}&category=${encodeURIComponent(category)}&sort=${sort}`), api.get('/categories')]).then(([p, c]) => { setProducts(p.data.data.products); setTotal(p.data.data.pagination.total); setCategories(c.data.data.categories); }).catch(() => {}).finally(() => setLoading(false)); }, [search, category, sort]); return <div className="mx-auto max-w-7xl px-5 py-12 lg:px-8"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="eyebrow">The collection</p><h1 className="section-title">Shop all</h1><p className="mt-2 text-sm text-ink/55">{total} considered pieces for daily life.</p></div><div className="flex items-center gap-2"><SlidersHorizontal size={17} /><select value={sort} onChange={e => update('sort', e.target.value)} className="rounded-full border border-ink/15 bg-white px-4 py-2 text-sm font-bold outline-none"><option value="newest">Newest</option><option value="popularity">Most loved</option><option value="price_asc">Price: low to high</option><option value="price_desc">Price: high to low</option></select></div></div><div className="my-8 flex gap-2 overflow-x-auto pb-1"><button onClick={() => update('category', '')} className={`pill ${!category ? 'pill-active' : ''}`}>All</button>{categories.map(c => <button key={c._id} onClick={() => update('category', c.name)} className={`pill ${category === c.name ? 'pill-active' : ''}`}>{c.name}</button>)}</div>{loading ? <div className="grid grid-cols-2 gap-4 md:grid-cols-4">{[1,2,3,4].map(i => <div key={i} className="skeleton aspect-[4/5] rounded-2xl" />)}</div> : products.length ? <div className="grid grid-cols-2 gap-x-4 gap-y-10 md:grid-cols-3 lg:grid-cols-4">{products.map(p => <ProductCard key={p._id} product={p} />)}</div> : <div className="rounded-2xl bg-[#f2f1eb] py-20 text-center"><h2 className="font-display text-3xl">Nothing here yet.</h2><p className="mt-2 text-sm text-ink/55">Try another search or browse all pieces.</p><button onClick={() => setParams({})} className="btn-primary mt-6">Clear filters</button></div>}</div>; }
