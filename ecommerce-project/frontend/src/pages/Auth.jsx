import { useState } from 'react'; import { Link, useLocation, useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext';
export default function Auth({ register = false }) { const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const { login, register: signUp } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const submit = async e => { e.preventDefault(); setError(''); try { register ? await signUp(form) : await login(form); navigate(location.state?.from || '/'); } catch (err) { setError(err.response?.data?.message || 'Please check your details and try again.'); } }; return <div className="grid min-h-[70vh] place-items-center px-5 py-16"><div className="w-full max-w-md"><div className="mb-8 text-center"><p className="eyebrow">Welcome to nexa</p><h1 className="mt-2 font-display text-5xl">{register ? 'Make yourself at home.' : 'Good to see you.'}</h1><p className="mt-3 text-sm text-ink/55">{register ? 'Create an account to keep track of the good stuff.' : 'Sign in to see your bag and orders.'}</p></div><form onSubmit={submit} className="grid gap-4">{register && <label className="field">Name<input required value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} /></label>}<label className="field">Email<input required type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} /></label><label className="field">Password<input required minLength="6" type="password" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} /></label>{error && <p className="rounded-lg bg-red-50 p-3 text-sm text-red-600">{error}</p>}<button className="btn-primary mt-2 justify-center">{register ? 'Create account' : 'Sign in'}</button></form><p className="mt-6 text-center text-sm text-ink/55">{register ? 'Already have an account?' : 'New around here?'} <Link className="font-bold text-coral" to={register ? '/login' : '/register'}>{register ? 'Sign in' : 'Create an account'}</Link></p></div></div>; }
